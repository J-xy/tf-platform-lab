(1) Why LockID, and what replaces it now that you use use_lockfile = true. 
(2) Recovering a lock after an apply crashes mid-run: what you check before force-unlock, and why. 
(3) Why terraform.tfstate is a secret: name three kinds of attribute that land in it in plaintext. 
(4) What breaks with two concurrent applies and locking off.
